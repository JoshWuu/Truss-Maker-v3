{
  "buildCommand": "cd truss-builder && npm install && npm run build",
  "outputDirectory": "truss-builder/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
